"""Create a tuple screen_resolution and set it to (1920, 1080). Unpack it into width, height = screen_resolution. Print a formatted string using those unpacked variables."""

screen_resolution = (1920, 1080)
width, height = screen_resolution
print(f"The screen resolution is {width}x{height}.")
